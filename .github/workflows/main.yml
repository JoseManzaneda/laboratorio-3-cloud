name: Explorando el Entorno

on:
  push:
    branches:
      - main

jobs:
  exploracion:
    runs-on: ubuntu-latest
    steps:
      - name: Imprimir un saludo
        run: echo "¡Hola Mundo!"

      - name: Dime la fecha actual
        run: date

      - name: Muestra el contenido del directorio
        run: ls -la